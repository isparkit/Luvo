{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<section class="faq-accordion-section color-{{ section.settings.color_scheme }} section-{{ section.id }}-padding" id="faq-accordion-{{ section.id }}">
  <div class="page-width">
    <div>
    {% if section.settings.heading %}<h2 class="faq-title">{{ section.settings.heading }}</h2>{% endif %}
      {% if section.settings.subheading != blank %}
        <p class="faq-subtitle">{{ section.settings.subheading }}</p>
      {% endif %}
    </div>
  <div class="faq-container">
    <div class="faq-left">
      {% if section.settings.faq_image %}
        <img src="{{ section.settings.faq_image | img_url: '600x' }}" alt="FAQ Image">
      {% endif %}
    </div>
    <div class="faq-right">

      <div class="faq-items">
        {% for block in section.blocks %}
          <div class="faq-item" id="faq-{{ section.id }}-{{ forloop.index }}">
            <button class="faq-question" aria-expanded="false">
              {{ block.settings.question }}
              <span class="faq-icon"></span>
            </button>
            <div class="faq-answer">
              <p>{{ block.settings.answer }}</p>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
  </div>
</section>

{% schema %}
{
  "name": "Custom FAQ Accordion",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Schema",
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Everything You Need to Know About Luvo"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Section Subheading",
      "default": "From battery life to compatibility, here are answers to the most common questions about Luvo Smart Ring."
    },
    {
      "type": "image_picker",
      "id": "faq_image",
      "label": "Left Side Image"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "faq",
      "name": "FAQ",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Custom FAQ Accordion",
      "category": "Custom"
    }
  ]
}
{% endschema %}

<style>
 .faq-container {
  display: flex;
  flex-wrap: wrap;
  gap: 30px;
  margin: 0 auto;
  align-items: flex-start;
} 
.faq-left img {
    max-width: 462px;
    min-height: 422px;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
}
.faq-right {
  flex: 1;
  min-width: 300px;
}
.faq-title {
  font-size: 28px;
  font-weight: bold;
  margin-bottom: 10px;
}
.faq-subtitle {
  font-size: 16px;
  margin-bottom: 20px;
  opacity: 0.8;
}
.faq-item {
  border-bottom: 1px solid rgba(255,255,255,0.1);
}
.faq-question {
  width: 100%;
  background: none;
  border: none;
  text-align: left;
  padding: 15px 10px;
  font-size: 18px;
  font-weight: 500;
  color: #fff;
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: pointer;
}
.faq-answer {
  display: none;
  padding: 0 10px 15px 10px;
  font-size: 16px;
  opacity: 0.9;
}
.faq-question.active + .faq-answer {
  display: block;
}
.faq-icon {
  transition: transform 0.3s ease;
}
.faq-question .faq-icon::before {
  content: "+";
}
.faq-question.active .faq-icon::before {
  content: "-";
}
</style>

{% comment %} <script>
document.addEventListener("DOMContentLoaded", function () {
  const faqSections = document.querySelectorAll(".faq-accordion-section");

  faqSections.forEach(section => {
    const questions = section.querySelectorAll(".faq-question");

    questions.forEach(btn => {
      btn.addEventListener("click", () => {
        const expanded = btn.getAttribute("aria-expanded") === "true";
        
        // Close all in this section
        questions.forEach(q => {
          q.classList.remove("active");
          q.setAttribute("aria-expanded", "false");
          q.nextElementSibling.style.display = "none";
        });

        if (!expanded) {
          btn.classList.add("active");
          btn.setAttribute("aria-expanded", "true");
          btn.nextElementSibling.style.display = "block";
        }
      });
    });
  });
});
</script> {% endcomment %}


<script>
document.addEventListener("DOMContentLoaded", function () {
  const faqSections = document.querySelectorAll(".faq-accordion-section");

  faqSections.forEach(section => {
    const questions = section.querySelectorAll(".faq-question");

    questions.forEach((btn, index) => {
      btn.addEventListener("click", () => {
        const expanded = btn.getAttribute("aria-expanded") === "true";
        
        // Close all in this section
        questions.forEach(q => {
          q.classList.remove("active");
          q.setAttribute("aria-expanded", "false");
          q.nextElementSibling.style.display = "none";
        });

        if (!expanded) {
          btn.classList.add("active");
          btn.setAttribute("aria-expanded", "true");
          btn.nextElementSibling.style.display = "block";
        }
      });

      // First FAQ default open
      if (index === 0) {
        btn.classList.add("active");
        btn.setAttribute("aria-expanded", "true");
        btn.nextElementSibling.style.display = "block";
      }
    });
  });
});
</script>